# Dataset & POC Audit

> **SUPERSEDED RULES (Phase 6 consolidation, 2026-09-24).** The following earlier rules are void wherever they appear below; the scientific measurements they gated are kept.
> - Requirement of *natural data owners*, *real hardware*, or *one linked Android+IoT dataset* (G-PHD-4, G-PHD v1/v2 "hard conjunction", "NO ELIGIBLE PROJECT" verdicts). Replaced by: simulated, deployment-motivated clients on public data are acceptable (no arbitrary APK shards); PhD relevance = clear link to >=1 thesis theme, used as a hard *filter* not a conjunction.
> - Verdicts "NO STRONG CANDIDATE" (Phase 5) and "no winner" — superseded by the Phase 6 classification in `Final Report.md`.
> - G-MECH/G-NOVEL remain in force as evidence standards.

## PHASE 6 — datasets and POC results (2026-09-24)

**Data reuse.** LAMDA x AndroZoo cache regenerated by `temp/p3_lamda_markets.py` -> `p3_build_lamda.py` (scratch caches live outside Git). Single-market rows per client: Play<=2018 297,387 (74,220 malware); Play>=2019 342,209 (18,965 mal); Anzhi 104,287 (90,801 mal, 13,486 benign); AppChina 88,282 (73,578 mal, 14,704 benign); VirusShare 71,379 (99% malware, only 430 benign -> unusable as client, no calibration benign).
**Family structure (single-market malware, AVClass2):** 346,761 labelled malware rows; top families dowgin 31,318, kuguo 20,192, airpush 14,990, smsreg 14,748, dnotua 10,146, gappusin 8,592. Families are strongly market-concentrated (dowgin 27,874 in Anzhi vs 114 in Play<=2018 block; airpush 9,485 Play vs 297 Anzhi; dnotua 9,310 Play vs 45 Anzhi; zdtad 2,566 Anzhi vs 3 Play; inmobi 4,286 Play vs 1 Anzhi) -> natural complementary exposure; most are adware libraries, so "family" != behavioural family (limitation).

**P60 (B) POC** — scripts `temp/p6_unseen_family.py` (arms), `p6b_nofam.py` (no-family-anywhere control), `p6_unseen_family_analyze.py`; outputs `p6_unseen_family_s{0-4}_n6000_a0.05.json`, `s{0-2}_n1500`, `p6b_nofam_s{0-4}_n6000.json`. Harness: `p3_engine.Eng` (MLP 128, FedAvg 30 rounds x 3 epochs, FedProx mu=.05, FT 3 epochs lr 5e-4), GPU, ~1 min/seed. Full table and interpretation: `Final Report.md` Phase 6. Rerun: `python3 p6_unseen_family.py SEED [N] [ALPHA]`; `python3 p6b_nofam.py SEED`.
Extra numbers: n=1500 (3 seeds) unseen recall local .411, central .638, FedAvg .602, FedProx .621, FT .617, oracle central .643; worst-client local .200, FT .270, central .263. nofam control (5 seeds): central .623 / worst .273 / known .799; FedAvg .591 / .314 / .765. Per-family (seed 0): dowgin local .51 -> central .99; hiddad .14 -> .43; smsreg .34 -> .38; gappusin .37 -> .38; youmi .26 -> .30.
Known limits: fixed group partition per seed (4 families/client); unseen malware test rows come from all four markets (cross-market shift confound, equal across arms); one architecture; static features; alpha .05 only; family label = AVClass2; per-seed worst-client sd .05-.12; eligible-family filter in code (>=150 peer train malware, >=50 test) leaves all 16 families eligible in seed 0.

**P61 (CASE extension) POC** — `temp/p6_case_calibrated_policy.py`, `p6_case_calibrated_policy_results.json`. Package-grouped 60/20/20 (train/cal/test), 5 seeds, SELENE both directions, pooled L2 logistic (source flags, counts, volume, claim one-hot) vs smoothed per-indicator persistence; threshold on calibration for target non-recurrence risk r* in {.05,.10}; pooled vs per-indicator threshold. Mean over seeds (r*=.05): 10->14 persistence pooled risk .052/cov .836/worst-ind .198; rich pooled .049/.873/.210; rich per-indicator .023/.709/.063. 14->10 persistence .055/.639/.098; rich pooled .052/.709/.223; rich per-indicator .042/.642/.060. r*=.10: pooled worst-indicator risk .36-.54 (cov .89-1.0) vs per-indicator .13-.22. Limits: one dataset, two directions, worst-indicator over indicators with >=30 kept test claims, per-indicator thresholds need >=50 calibration claims, indicators partly redundant.

**P62 (MIA) POC** — `temp/p6_mia.py`, `p6_mia_s{0-2}_n{500,3000}.json`: per-client AUC of loss-threshold attack, local/FedAvg/central .48-.58, no arm reliably above .55; n=3000 all <=.54.

**Consolidation record.** poc2 scripts (`c1_*`, `c2_leakage_scidb*`, `c3_audit*`, `c4_repack*`, `v1_nogate`, `v2_gate`, `v3_peer`, `v4_peer2`) moved to `temp/`; progress log is `temp/progress.txt`.



---

## Dataset Audit (poc)

*Source: `poc/Dataset Audit.md` (merged 2026-09-24). *

### CASE-Android Dataset Audit

Audit snapshot: 2026-09-23. This file records files inspected locally, metadata checked online, and access or semantic limits. It does not alter `docs/Roadmap.md`.

#### Dataset disposition

| Dataset | Access and inspected artifact | Identity/pairing | Context and fields | CASE decision |
|---|---|---|---|---|
| SELENE / ARTEMIS | Eight compact Parquet files already present in the shared data cache; 29,777,711 bytes total. | 30,746 exact SHA-256 pairs. Run-feature hashes are unique within each version. 22,475 package groups among paired rows. | Android 10/API 29 and Android 14/API 34; both `strace`, Monkey, and `completed_behavior`. Released L1 flags plus event counts, volumes, duration, event timing, ratios, and derived summaries. | **PRIMARY** for semantic claim-level CASE. Restrict claims to finite, processed evidence in these two emulator contexts. |
| KronoDroid | Public four-file CSV release streamed and parsed in memory from the maintainer repository; 25,679,248 compressed bytes across four archives. | SHA-256 is present. 63,320 intersecting hashes before duplicate handling; **63,316** one-to-one pairs after excluding four duplicate-hash identities per context. No malware/benign disagreement remains among valid pairs. Family labels disagree on 33,838/63,316 pairs. | Emulator versus real-device syscall count profiles, 288 syscall fields plus `nr_syscalls`, static/manifest fields, and APK metadata. The 288 dynamic fields have identical names/order and no missing values in the downloaded rows. | **CORE REPLICATION / SECONDARY** for direction-specific recurrence and broken-pair validation. Do not use this release for family-OOD claims without label reconciliation. |
| AndroCT | Zenodo metadata and README inspected; 6.3 GB release. Per the release, reuse requires a faculty member or person in a permanent position to agree to its terms; no redistribution or commercial use. No trace archive extracted in this audit. | The release describes 35,974 apps with emulator and real-device traces; this is the **nominal** pairing count. Exact APK-hash join and duplicate count remain unverified from the released archive. | Ten-minute Monkey-driven method-call traces on an emulator and Samsung Galaxy S4. Method-call edges carry full method signatures. Same Android platform version and same-length/input-coverage criteria are described. 18,277 benign and 17,697 malicious apps. | **OPTIONAL / BLOCKED pending agreement and artifact-level identity audit.** Very useful for abstraction/granularity follow-up; high direct novelty collision for cross-device behavior differences. |
| DYNAMISM 2016–2023 | Zenodo record is public but files are marked **Restricted**; no archive was accessible locally. | Maintainer metadata says filenames preserve AndroZoo hashes. Same-app 2nd-run subset: 1,049 benign and 772 malware. | Android 9 container; 20,000 Monkey events or five-minute limit; one-second aggregates. First-run filtered set: 12,706 benign and 10,560 malware; failed runs are numerous and class-skewed. | **DIAGNOSTIC / BLOCKED** until file access is granted. Best candidate for same-context execution-instability comparison if authorized. |
| CIC-InvesAndMal2019 | Official UNB dataset page inspected; files not present in shared cache, no state tables extracted. | Page gives no stable SHA-256/run-identity contract for joining `AfterInstall`, `Before`, and `After`. | Three captures around reboot on physical devices; API calls, logs, network flows, battery/process/package data. | **BLOCKED** as a CASE dataset until exact app and comparable-feature pairing is verified. Publisher numbers conflict: “5,000” installed samples versus 426 malware + 5,065 benign (=5,491). |
| TraceDroid | Local shared cache contains 85 files totaling 45,181,835 bytes, including historical per-year profile tables and feature archives. | SHA-like APK filenames are present in annual profiles. No same-APK paired execution contexts are described by the dataset record. | Historical static/dynamic profiles for apps from 2010–2017; function-call profiles were collected under a single analysis setup. | **DIAGNOSTIC / DEFER** for historical cohort or feature drift only. It does not estimate same-APK cross-context recurrence. |
| AndroZoo metadata | Shared cache has a multi-gigabyte latest metadata dump; it is an APK metadata catalog, not a set of paired dynamic traces. | APK hashes and dates support cohort definitions, not target dynamic evidence. | Metadata only. | **REJECT** as a CASE outcome dataset; may be used only to resolve external metadata, subject to access terms. |
| AndroidMischief / AntiARA / other local Android artifacts | Local directories exist, but the initial file inventory found no paired, comparable same-APK context traces for the CASE estimand. | No verified cross-context pair manifest. | Malware action/evasion artifacts, not a paired report-evidence table. | **DIAGNOSTIC** for background only; do not add absent a specific paired estimand. |

#### SELENE / ARTEMIS: direct artifact audit

The local external cache is `/home/naslouby/Projects/datp-shared-data/raw/SELENE` (not part of this repository and not copied into it). The eight files and their row counts are:

| Artifact | Android 10 rows | Android 14 rows | Fields observed | Bytes, A10 / A14 |
|---|---:|---:|---|---:|
| `analyses` | 44,485 | 30,751 | 20 each: exact hash/run IDs, Android/API, observer, input method, status, duration, trace and source artifact metadata | 3,876,547 / 2,686,400 |
| `run_features` | 44,485 | 30,751 | 82 each; includes 19 `has_*` flags, semantic event counts, event totals/diversity/severity buckets, first/last event time, active duration, syscall/resource counts, ratios, and derived behavior scores | 9,990,871 / 7,296,134 |
| `fidelity_oracle` | 1,000 | 1,000 | 50 each; raw artifact availability, raw evidence and signals for parser/fidelity checks | 1,389,463 / 1,755,546 |
| `fidelity_evidence` | 49,345 | 54,944 | 9 each; signal, artifact path/type, line and matched text | 1,237,029 / 1,545,721 |

The eight local Parquet files total **29,777,711 bytes**. Their SHA-256 checksums are captured in `temp/progress.md`. This exact local inventory is smaller than the complete public SELENE artifact card: no `events_l05`, `events_l1`, lifecycle tables, or context-representation files were present in this cache. The public release card describes 73,635,326 / 86,050,148 L0.5 events and 73,673,283 / 82,843,224 L1 events in the two contexts, but those layers were not locally audited row by row here.

Actual `analyses` values: all Android 10 rows are version 10/API 29 and all Android 14 rows are version 14/API 34; both contexts report `strace`, Monkey, and `completed_behavior`. Median run duration is **188.33 seconds** in Android 10 and **143.79 seconds** in Android 14 (ranges 25.56–659.82 and 21.01–651.74 seconds). This difference is a plausible measurement-exposure covariate; it prevents causal attribution of observed differences to Android version alone.

The paired run-feature population is 30,746 hashes (30,746 shared; 13,739 Android-10-only; 5 Android-14-only). The paired Android-10 feature distribution is selected: versus Android-10-only rows, mean absolute indicator-prevalence difference is **3.20 percentage points**, and the maximum absolute difference is **8.72 points**. This is material for external validity and must appear in the main report.

Across all 30,746 paired APKs × 19 flags, the four-state table is: `n00` stable absent **148,629**, `n01` target-emergent **57,745**, `n10` source-only **37,919**, `n11` stable present **339,881**. Thus micro recurrence is 89.96% in 10→14 and 85.48% in 14→10; target-emergence probability among source-absent claims is 27.98%. Indicator-macro recurrence is 85.52% / 79.63%, and indicator-macro emergence among source-absent cases is 50.99%. These descriptive numbers support keeping emergence as a distinct secondary estimand; they do not support treating a source non-observation as a proof of target absence.

The fidelity oracle has 1,000 records per context but only **56 shared hashes**. It can support limited parser/provenance auditing, not independent ground truth or broad context-level risk guarantees. Fidelity-evidence text and paths may expose raw strings; only aggregate summaries should be published.

##### Source-only predictor eligibility

For the PoC, source-side input was restricted to the source `run_features` row: the 19 released flags, released semantic/syscall count fields, total event/activity fields, and source duration. No target-side fields, target metadata, hashes, package names, malware labels, or family labels entered the model. Hash is used only to form the one-to-one pair; `package_name` is used only as the grouping variable. `family`, selection/run identifiers, derived triage scores, and raw fidelity text are excluded from predictors.

The 19 flags are not 19 independent behaviors. At least these overlaps are visible in the released table: `has_NETWORK_EXTERNAL` and `has_external_tcp` match exactly in source context; `has_PROCESS_MEMORY_READ` and `has_process_vm_readv` also match; the two `*_high_volume` flags are threshold-derived from count columns. Future evaluation should retain the released 19-flag result for reconciliation and add a predeclared broad/non-derived sensitivity. It must not interpret the 19-row indicator macro as 19 independent discoveries.

**Access/licensing:** the public SELENE card specifies a dedicated SELENE data license plus the upstream ARTEMIS license and mandatory citation to both papers. The card states that raw malware-controlled endpoints, paths, identifiers, and credential-shaped strings are not redacted. Respect both licenses and keep raw strings out of public artifacts. The roadmap currently describes its earlier license characterization; reconcile exact license text and citations against the live dataset card before protocol freeze.

#### KronoDroid: actual CSV findings

The four public CSV archives were fetched from the maintainer GitHub release and parsed in memory; no APKs or extracted dataset files were retained. The archive sizes are approximately 6.1 MB emulator-benign, 5.4 MB emulator-malware, 5.8 MB real-device-benign, and 8.3 MB real-device-malware. The full four-archive extraction is practical for CPU-only local analysis; the audit completed in seconds on the current WSL host. Peak RAM was not measured.

| Check | Actual result |
|---|---|
| Rows | Emulator: 35,246 benign + 28,745 malware = 63,991. Device: 36,755 benign + 41,382 malware = 78,137. |
| Total schema | 484 columns in concatenated contexts. The release README advertises 200 static + 289 dynamic (=489), which does not reconcile with these current CSVs. Emulator benign versus malware schemas also differ by `FilesInsideAPK`. Resolve this count discrepancy before implementation. |
| Dynamic schema | Exactly 289 aligned columns in the same order across contexts; `nr_syscalls` is an aggregate field, leaving 288 syscall count columns. No missing values were found in those dynamic fields. |
| Identity | Exact `sha256` field in both contexts. Raw overlap is 63,320 hashes. There are four duplicate-hash groups in each context, each with conflicting `Malware` labels; excluding non-unique identities yields 63,316 one-to-one pairs. All remaining paired malware/benign labels agree. |
| Label caveat | `MalFamily` disagrees on 33,838 of 63,316 unique-hash pairs (46.6% agreement). Do not use family identity for OOD splits until this is explained. |
| Time fields | The inspected CSVs expose APK metadata fields such as `FirstModDate`/`LastModDate` or `EarliestModDate`/`HighestModDate`, plus `TimesSubmitted`. Names differ by context. These are not verified as execution/capture timestamps; they do not yet validate portability drift or future-period evaluation. |

Syscall presence was defined as count > 0, yielding app × syscall recorded-evidence claims. On exact pairs, emulator→device recurrence was **59.14%** among 2,328,191 source-positive app/syscall claims; reverse recurrence was **68.59%** among 2,007,336. The support≥100 syscall-macro recurrence was 50.04% and 57.70%. Malware/benign pooled recurrence was 63.20% / 55.75% emulator→device and 65.31% / 72.01% in reverse. These are descriptive: features are system-call occurrences, not semantically named security claims.

The broken-pair negative control randomly permuted target APK rows. Pooled recurrence then fell to 51.84% and 60.11%; true pairing therefore adds about **7.29** and **8.48 percentage points** over that shuffled control. This supports a genuine same-APK association, but it does not establish a predictive CASE model or report-scoping benefit.

#### Other candidate audit notes

##### New 2025–2026 candidate check: IoT and federation alignment

These datasets were checked after the initial Android-focused audit because the user’s PhD alignment explicitly includes heterogeneous IoT collaboration. This is a metadata/literature audit only; no new IoT archive was downloaded or reprocessed in this pass.

| Dataset | Current evidence | Client / label validity | Fit and decision |
|---|---|---|---|
| **Crowdsensing IoT malware dataset (Scientific Data, 2026)** | The descriptor reports 8 Raspberry Pi 3/4 devices, 342,106 30-second records from 288 hours, 32 selected features, a 687-feature processed representation and raw behavior logs; files are organized by device/label. The Science Data Bank record embeds **331,750,002,920 bytes** (≈331.75 decimal GB / 308.97 GiB), while its page description says 308.1 GB; treat the rounded display as inconsistent. Its dataset record metadata says **CC BY 4.0**; the article itself uses CC BY-NC-ND 4.0, so cite/use the dataset under its record terms and cite the article separately. The dataset already evaluates centralized FL and decentralized FL, multiple topologies and data distributions. Data are linked through the [paper](https://www.nature.com/articles/s41597-026-07155-w), [dataset DOI](https://doi.org/10.57760/sciencedb.25380), and [PMC record](https://pmc.ncbi.nlm.nih.gov/articles/PMC13219454/). | Device is a stronger client key than arbitrary APK sharding. However, the eight devices belong to one controlled research testbed, not independent organizations; they are testbed endpoints, not naturally independent data owners. 30-second windows are dependent and must be grouped chronologically/device before split. The paper says global min–max preprocessing was used, which may leak test distribution if recomputed globally. | Strongest near-term **PhD dataset lead**, but DFL baseline has already been published. A minimally sufficient follow-up would require only the 32-feature processed files for all eight devices and nine labels; their combined size was not exposed in the dataset page’s metadata. Do not fetch the 332 GB raw+processed collection. Obtain the processed-folder file manifest and exact size first, then reassess a small honest-device POC (e.g. held-out-device selective calibration versus FedAvg/local baselines). Linux Raspberry Pi behavior is not Android mobile-app reporting. **Metadata-only / DEFER.** |
| **N-BaIoT** | Public nine-device smart-home network traffic under benign and Mirai/BASHLITE botnet activity; device IDs permit natural device-level partition experiments. [Original dataset paper](https://arxiv.org/abs/1805.03409); current FL malware studies use it, including [device-level client setup](https://www.mdpi.com/2079-9292/14/6/1089). | Device partition is natural as an endpoint but only one study’s dataset capture, not multiple data custodians. Outcomes are botnet traffic/compromised-device detection, not APK malware or mobile app report claim. | Suitable as a thesis benchmark; highly collided and dated; no direct book-chapter CASE fit. **Do not switch core project.** |
| **ToN-IoT** | Official UNSW page describes telemetry, network, Windows/Linux sources and multiple testbed systems/attacks; [official dataset page](https://research.unsw.edu.au/projects/toniot-datasets). | Several modalities/sources, but a common lab acquisition. “Source” or device partitions do not prove separate organizational ownership, and attack labels are not Android app labels. | Useful broad IoT FL benchmark; less directly malware-specific than current CASE question. **Alternative PhD work only.** |

The new IoT dataset makes a thesis-aligned empirical line more feasible than the Android corpus does, but does not change the chapter recommendation: it does not contain Android packages, mobile-app permissions/behavior reports, or CASE’s cross-context claim ontology. Treat PhD work and the mobile-security chapter as related but separate deliverables unless a future dataset genuinely links the settings.

##### Mobile/IoT convergence candidate and client audit (P11)

KronoDroid supplies a defensible early **simulated** client boundary: the same Android APK executes under an emulator acquisition context and a physical-device collection context. The pair is kept together by exact SHA for split assignment, then appears at both context clients only within that single partition. This can test whether a mobile malware model behaves consistently across two named collection domains; it cannot establish independent owners, users, IoT network endpoints, or privacy protection. It must not be described as a natural cross-silo federation. The 63,316 exact paired identities and 288 released syscall-count features are verified above; family labels are excluded because 33,838 pair labels conflict.

P11 is PHD_PARTIAL: malware detection and an Android app-security task are present, but the current public data contain no IoT network observation, no mobile/IoT deployment mapping, and no privacy intervention. The three-seed model POC is reported in [PoC Matrix.md](PoC%20Matrix.md). It finds local-only > FedAvg/FedProx, severe opposite-context transfer failure, and a failed agreement-only CASE feature mask. Therefore the currently evidenced CASE signal does not improve collaborative detection. The 2026 Raspberry Pi data provide device clients for the IoT side but do not connect those clients to Android app behavior or mobile-app privacy. No dataset presently closes both sides of the hard eligibility gate.

**Other data lead:** DEFEAT Android APT physical-device behavior data (Mendeley Data, CC BY 4.0) appear to cover device telemetry and attack stages, but the inspected record points to a single physical device/collection and does not establish a multi-client client topology. Treat it as a mobile-security artifact lead, not a federation-ready dataset, until its file manifest, exact device count, labels, and partition units are verified. Dataset record: [DEFEAT Android APT](https://data.mendeley.com/datasets/bdtn9vj7d7/3).

One additional validity gate applies to this candidate: the paper describes scripts that repeatedly trigger representative malware behaviors. Before naming the target “real-world malware-family generalization,” audit the labels, malware code and condition-generation pipeline; until then the defensible outcome is classification of the recorded benign/attack conditions in this controlled testbed.

##### AndroCT

The current Zenodo record is [10.5281/zenodo.6336104](https://zenodo.org/records/6336104); the archive is 6.3 GB. Its README describes per-app traces under both context families, 10-minute execution, Android Monkey, emulator and Samsung Galaxy S4, with 35,974 unique apps across 2010–2019. The public terms require a faculty/permanent-position agreement, prohibit redistribution and commercial use, and require citation. Because that agreement was not verified, do not treat the artifact as an authorized CASE dataset in this audit. It is still the strongest optional granularity follow-up: method edges can be grouped to class/package/API family, but the same fact makes “emulator/device behavior differs” a direct novelty collision.

##### DYNAMISM

The current [Zenodo record](https://zenodo.org/records/21280255) is explicitly Restricted. Its metadata are unusually well matched to the same-context instability question: Android 9, 1-second summaries, repeated traces for 1,821 apps, and original AndroZoo hashes in filenames. However, run-level pairing, feature schemas, and missingness cannot be checked until the files are accessible. The class-skewed run failures make complete-case selection a prominent risk.

##### CIC-InvesAndMal2019

The [official UNB page](https://www.unb.ca/cic/datasets/invesandmal2019.html) defines AfterInstall, Before, and After captures, including around restart. It gives a contradictory sample total and no audited exact identity key or comparable-feature manifest. Keep the reboot idea as a promising blocked lane; do not infer stage pairing from matching folder names.

##### TraceDroid and related datasets

The [TraceDroid Zenodo record](https://zenodo.org/records/3665877) covers historical app profiles and static/dynamic function-call data, but it is not a repeated same-APK/multi-context release. It can inform a separate historical cohort question after same-hash and feature semantics are verified; it cannot substitute for the recurrence outcome. Android-in-the-Wild is a device-control interaction dataset rather than a dynamic security-evidence dataset. Android device-specific behavior studies are important prior work, not fresh evidence for CASE. The local AndroidMischief and AntiARA directories contain action/evasion material but no verified paired same-APK evidence table in the inspected inventory.

#### References checked

- [SELENE Android Paper Artifacts dataset card](https://huggingface.co/datasets/serrooT/selene-android-paper-artifacts) and [dataset README/license and limitations](https://huggingface.co/datasets/serrooT/selene-android-paper-artifacts/blob/main/README.md).
- [AndroCT current Zenodo record](https://zenodo.org/records/6336104) and [published MSR 2021 dataset paper](https://doi.org/10.1109/MSR52588.2021.00076).
- [KronoDroid repository and release README](https://github.com/aleguma/kronodroid) and [dataset paper](https://doi.org/10.1016/j.cose.2021.102399).
- [CIC-InvesAndMal2019 official data page](https://www.unb.ca/cic/datasets/invesandmal2019.html).
- [DYNAMISM 2016–2023 Zenodo record](https://zenodo.org/records/21280255).
- [TraceDroid Zenodo record](https://zenodo.org/records/3665877).


---

### Phase 3 addendum — datasets audited for a G-PHD-eligible project (2026-09-23)

Inputs were read from the local shared cache `/home/naslouby/Projects/datp-shared-data/raw/` (not copied into Git). Scripts and aggregate outputs: `temp/p3_lamda_markets.py`, `p3_build_lamda.py`, `p3_lamda_v2.py`, `p3_nbaiot_v2.py`, `p3_analysis_output.txt`. Earlier SELENE / KronoDroid / AndroCT / DYNAMISM / CIC / TraceDroid / SCIDB-IoT audits above remain valid and were reused, not rerun.

#### D-P3-1. LAMDA (features/labels/time/family) joined to AndroZoo `latest.csv.gz` (markets)  — PRIMARY for Phase 3

| Field | Finding |
|---|---|
| dataset | LAMDA longitudinal Android malware benchmark (IQSeC, UTEP; MIT licence per dataset card) + AndroZoo catalogue (`latest.csv.gz`, 3.52 GB) |
| source | local caches of `huggingface.co/datasets/IQSeC-Lab/LAMDA` and AndroZoo; paper arXiv 2505.18551 |
| license | LAMDA: MIT (card). AndroZoo: usage terms/citation required and APK redistribution restricted; re-verify before publishing derived lists |
| actual accessible files | `LAMDA/metadata.csv` (1,008,381 rows), `var_thresh_0.01/<year>/*_{train,test}.parquet` (920 binary static features), Baseline (4,561 features), NPZ variants, `AndroZoo/latest.csv.gz` |
| size | features (var_thresh_0.01) 138 MB; metadata 124 MB; AndroZoo catalogue 3.5 GB |
| security task | binary malware vs benign (VirusTotal ≥ 4 detections) and AVClass2 family |
| mobile relevance | Real Android APKs 2013-2025 (no 2015) |
| IoT/edge relevance | **None directly.** Static app features; the store/site is a crawl origin, not an edge device |
| malware labels | yes, AV-threshold label (AV-centric bias stated by the card) |
| client key | AndroZoo `markets` (pipe-separated). All 1,008,381 LAMDA hashes joined (100 %). 935,789 (92.8 %) are single-market |
| number of clients | 7 usable single-market clients with both classes and ≥ ~3k rows: play.google.com 639,596; anzhi 104,287; appchina 88,282; PlayDrone 6,232; slideme 4,448; 1mobile 4,018; angeeks 3,325. Config B replaces Play by five Play era cohorts (11 clients). Excluded: VirusShare (99.4 % malware), praguard, mi.com, unknown (≈ all malware), fdroid (46 malware), torrents/freewarelovers/proandroid (<25 malware) |
| client naturalness | **Natural site key, not a natural federation**: market = where the APK was crawled; not an independent organisation holding private data. Malware prevalence per market partly reflects AndroZoo collection policy (VirusShare is a malware repository). Config B eras are simulated (store × period) |
| heterogeneity dimensions | label prevalence (14.6 % Play, 87 % Anzhi, 83 % AppChina, 50 % PlayDrone, 36 % SlideMe, 61 % 1Mobile, 51 % Angeeks); feature JSD (mean per-feature JS ≈ 0.002-0.022 between markets; benign cross-market 0.011-0.015 vs 0.0021 one-year Play drift); sample-size imbalance; time span |
| sample identity | SHA-256 |
| APK identity | SHA-256 + `pkg_name` (used for package-grouped splits shared across all clients) |
| timestamps | `year_month` (dex/added time); calendar year used only descriptively |
| device identity | none |
| context identity | market only |
| family identity | AVClass2 family available; not used |
| feature compatibility | identical 920 binary features for every client |
| possible split | package-grouped 60/15/25 train/val/test per seed, assigned by hash(pkg_name, seed); train capped at NTR ∈ {100,300,1000,5000}, val = max(60, min(1500, NTR/3)), test ≤ 4000 |
| possible held-out-client test | yes (leave-one-market-out cold start, done) |
| possible privacy threat model | honest-but-curious server, label-flipping client, threshold/calibration manipulation (only label flip tested) |
| possible CASE relation | none (no dynamic evidence, no context pairs) |
| POC feasibility | feasible and run (GPU, seconds per seed) |
| fatal issues for G-PHD | no IoT/edge semantics (G-PHD-4); market prevalence is a collection artifact so prevalence heterogeneity may overstate deployment heterogeneity; same-package apps across markets controlled only through pkg-grouped splits; static features only; AV-label bias; VirusTotal labels shift over time |

Market table produced by `p3_lamda_markets.py` / `p3_build_lamda.py` (aggregate counts only).

#### D-P3-2. N-BaIoT (device clients, IoT replication)

| Field | Finding |
|---|---|
| source / licence | UCI / BGU; citation mandatory (Meidan et al. 2018), verified in the shipped description file |
| accessible files | 9 device folders, `benign_traffic.csv` + `mirai_attacks/` + `gafgyt_attacks/` (RAR + extracted CSV), 115 statistical features |
| size | 9.1 GB local |
| task | benign vs botnet traffic (Mirai, BASHLITE/Gafgyt) |
| mobile relevance | none |
| IoT relevance | strong (9 commercial IoT devices) |
| client key / number | device; 9 (Danmini, Ecobee, Ennio, Philips baby monitor, Provision 737E/838, Samsung webcam, SimpleHome 1002/1003). Natural attack prevalence 84–98 % |
| naturalness | natural per-device data owner; single testbed |
| heterogeneity | device traffic distribution and prevalence |
| identity fields | no sample/APK identity; timestamps absent in CSV; device identity yes; family (attack file) yes |
| feature compatibility | identical 115 features |
| possible split | random within device (no identity leakage control possible) — **weakness** |
| POC | run: BA ≥ 0.995 for central/FedAvg/FedProx/FT at n ≥ 1000 (saturated) |
| fatal issues | saturated benchmark; no collaboration-scope problem to solve; no mobile link |

#### D-P3-3. McNdroid (features + 119 AV-vendor verdict columns)

Files: `metadata.csv` (858,859 rows: hash, date, label, family), `vendor_family_wide_verdict.csv` (770 MB, 119 vendor columns), `mcn_data.zip`, `mcn_json.zip`, `data_feature/processed_data/init_<year>/`. Inspected only the first 150,000 verdict rows (mostly benign, not representative): the 20 fully covered vendors have pairwise verdict agreement 0.81-1.00 (median 0.959). **Lane P51 (vendor-as-client label heterogeneity) not run**: all vendors see the same APK pool, so clients would need an arbitrary sample partition, which fails G-PHD-7 unless a deployment justification is written. Kept as a documented candidate.

#### D-P3-4. Other locally available or web-checked datasets (listing / metadata level only unless stated)

| Dataset | What was checked | Verdict for G-PHD |
|---|---|---|
| HITL-IoT | README (127,845 IoT flows, 12 device types, analyst annotations) | IoT-only, no mobile app; not audited further |
| Gotham2025 (78 emulated IoT devices) | README + web abstract | IoT-only |
| PingPong, TU Wien Philips Hue, moniotr | directory listing, README | IoT companion-app / smart-home traffic, **benign device events**, no malware label; PingPong links phone-app events to plug traffic but is not a malware dataset |
| AndroidMischief (Stratosphere) | README | 8 Android RAT pcaps from one phone; no device/site heterogeneity; no benign pool |
| TwinDroid, PARROT2025, MIRAGE, LLMAID | listing only | not audited; PARROT/MIRAGE are app-traffic, not malware-FL |
| CICMalDroid2020 / CCCS-CIC-AndMal2020 | web search | static+dynamic, no device/owner key |
| DYNAMISM 2016-2023, AndroCT, CIC-InvesAndMal | earlier audit | still restricted / agreement-gated / unresolved |
| Android Automotive / Android Things / Android TV malware, mobile↔IoT paired malware | three web searches (2025-2026) | **no dataset found** |

#### Data property that does not exist (summary)
No audited or discovered dataset combines (i) malicious mobile-app behaviour, (ii) natural multi-device/multi-owner client identity, and (iii) an IoT/edge semantic link. That triple is what G-PHD needs and is what blocks a natural winner.


---

### Phase 4 addendum — IoT device dataset (SciDB) audited directly (2026-09-23)

Scripts/outputs: `temp/p4_scidb_list.py` (manifest listing), `temp/p4_data.py`, `temp/p4_iot_v1.py`, `temp/p4_iot_v2.py`. Raw CSVs live in the session scratchpad only (not in Git). SHA-256 of the eight device files was computed locally (`sha256.txt` in the scratchpad).

#### D-P4-1. IoT intrusion detection dataset for decentralized federated learning (ScienceDB, DOI 10.57760/sciencedb.25380, v2.0.0)

| Field | Finding |
|---|---|
| dataset / source | Science Data Bank record; manifest read through the site's public file-tree API (`gin-sdb-filetree/public/file/childrenFileListByPath`, path `/V2`); files fetched with per-file download links (`china.scidb.cn/download?fileId=…`). No raw collection downloaded |
| license | record metadata CC BY 4.0 (article CC BY-NC-ND 4.0 — cite both, do not redistribute derived rows) |
| top-level manifest | `/V2/32d-feature data/{device/, merged_df.csv}` and `/V2/IoT intrusion detection dataset/{0_raw_collected_data, 1_syscall_processed_features}` |
| accessible files used | `32d-feature data/device/0.csv … 7.csv` (10.4–11.7 MB each, 85.7 MB total, sizes match the manifest exactly) and `merged_df.csv` (168,361,968 B) for audit only |
| security task | benign (label 0) vs 8 malware conditions (labels 1–8: Botnet, HttpBackdoor, Backdoor, TheTick, Beurk, Bdvl, XMRig, Ransomware per the record) |
| mobile relevance | none (Raspberry Pi Linux behaviour) |
| IoT/edge relevance | strong: 8 physical devices (Pi 4 ×2, Pi 3B ×6 per the record) |
| client key / number | one file = one device = one client; **8** |
| naturalness | natural per-device data source; one lab, controlled infection campaigns |
| schema | 31 float features (system-call rates, block/ext4/jbd2 kernel events, I/O and utilisation) + integer `label`; identical columns in all 8 files; no device id, timestamp, session or run id in any column |
| normalisation | every feature lies in [0,1] with min 0 and max 1 in every file: the release is min-max normalised (global normalisation, confirmed by the ranges); raw / pre-normalised values are not available in the 32-feature release. **Limitation kept**: no train-only scaling sensitivity is possible; we additionally z-scored features with federated-style pooled train statistics (POC) and also ran the released [0,1] values (sensitivity) |
| records | device files: 20,448 / 21,765 / 21,420 / 21,455 / 21,550 / 21,215 / 21,605 / 21,595 rows (171,053 total). `merged_df.csv` has 342,106 rows = exactly 2 × 171,053 (the paper's "342,106 samples" counts the merged file) |
| labels / coverage | all 9 labels occur on every device (≈2,400 rows each; some labels 1,775–2,820) — every malware condition occurs on every device |
| missing values | none |
| **duplicates (critical)** | every device file contains each distinct feature vector 5× (a few ×6 or ×12): only 4,042 / 4,024 / 4,284 … unique vectors per device (≈ 33,354 unique rows over all devices, ≈ 20 % of rows). No duplicate group spans two labels; no vector is shared across devices. Random splitting of raw rows would leak test rows into training; we **de-duplicate before splitting** |
| ordering / temporal | rows are shuffled (label runs of length 1–2); no time or run identity can be reconstructed. **Session/run and contiguous-block grouping are impossible**; 30-s windows from one infection run therefore remain statistically dependent across our random train/val/test split (documented limitation, likely optimistic) |
| unique windows per device × label | ≈ 235–485 (mostly ≈ 470–480) |
| heterogeneity | device-level feature and malware-condition score shift (transfer AUC/BA matrices in POC); prevalence is nearly identical across devices (≈ 89 % malware windows) so heterogeneity is not a label-prior effect |
| possible split | after de-duplication: random 60/15/25 train/val/test per device, train capped at {100,300,1000,full≈2,460} |
| possible held-out-client test | yes (not run in Phase 4) |
| CASE relation | none |
| POC feasibility | feasible and run (GPU, seconds per seed) |
| fatal issues | no run/session identity (dependence leakage, cannot be removed); globally min-max normalised release; single lab; 89 % malware prevalence makes a fixed 0.5 threshold degenerate (an argument for calibrated operating points, not a defect) |

Reconciliation: earlier Phase 2 note said "342,106 aggregated samples". Correct reading: 171,053 device rows, of which ≈ 4,100 unique vectors per device.


#### Phase 5 additions (2026-09-24)

- **LAMDA + AndroZoo markets (mobile):** 7 single-market clients, natural site key; new supervised FedAvg MLP scorer (`temp/p5_mobsup_scores.py`; 30 rounds x 2 epochs, 3000 train/class/client, package-grouped 60/15/25, per-client AUC .92-.99 over 5 seeds). Score caches in `temp/p5_scores/`. Market prevalence is natural; **calibration-pool contamination is injected** (random / top-score / low-score contaminants, hom 0-.20, linear 0-.3, prevalence x .3). LAMDA labels exclude VT grey (1-3), so no natural contamination.
- **N-BaIoT (9 devices), SciDB Pi (8 devices, de-duplicated):** federated AE scorers (`p5_scores.py`, reuse poc2 builders); pool 200 / 60 windows; injected contamination only.
- Limits: static features (mobile), no session ids (IoT); pools of 200 cannot resolve alpha=.01.


---

## Dataset Audit (poc2)

*Source: `poc2/Dataset Audit.md` (merged 2026-09-24). *

### poc2 Dataset Audit (2026-09-23)

Scripts: `temp/c3_audit.py`, `c4_repack_audit.py`, `c1_common.py`; outputs `temp/c3_audit_output.txt`. Raw data stays outside Git (`/home/naslouby/Projects/datp-shared-data/raw`, session scratchpad for the SciDB CSVs). All three datasets are used only through public releases; no real devices.

#### LAMDA + AndroZoo markets (mobile clients)
- 1,008,381 apps, 920 binary static features (variance-thresholded release), labels: benign = VirusTotal 0 detections, malware >= 4 (grey 1-3 excluded), so **no natural label contamination exists in the labels**; contamination in this study is simulated on top of real market structure.
- Market key from AndroZoo `markets`: 92.8% single-market. Seven usable clients: play (639,596 rows, 14.6% malware), anzhi (104,287; 87%), appchina (88,282; 83%), PlayDrone, slideme, 1mobile, angeeks. Market prevalence partly reflects AndroZoo collection policy, not deployment.
- Package identity: 10,664 packages appear in >=2 client markets; 5.9% of rows share a package with another client (angeeks 28.9%, appchina 27.1%, Play 1.5%). 22.8% of these cross-market packages carry conflicting verdicts (800 benign on Play but malicious elsewhere). Conflict pairs are more similar (cosine .53) than random pairs (.29) but less than benign-benign pairs (.71): repackaging-like. Splits are package-grouped.
- Families: 111,758 distinct AVClass2 families over 266,446 malware rows in the seven markets; top five cover 29.5% (dowgin, kuguo, airpush, dnotua, gappusin: mostly adware libraries). Only 4 families have >=150 samples in >=4 markets (airpush, gappusin, leadbolt, plankton). Family-level studies are therefore weak on this data.
- Frozen federated AE scorer quality per market (AUC benign vs malware): 1mobile .93, slideme .92, angeeks .79, PlayDrone .76, appchina .67, anzhi .57, play .45 (weak scorers for the largest clients).
- Limits: static features only, AV-centric labels, no device or timestamp-at-execution identity.

#### N-BaIoT (9 device clients)
- 115 statistical features, 9 devices, benign + Mirai/BASHLITE attacks (10 types on 7 devices, 5 gafgyt-only on Ennio and Samsung).
- **Exact-duplicate benign rows per device: Danmini 18.5%, Ennio 9.4%, Provision 737E 11.2%, SimpleHome 8.2%/8.2%, Philips 5.8%, Samsung 5.0%, Provision 838 4.6%, Ecobee 0%.** Benign rows are de-duplicated before splitting in the final runs; attack rows had no duplicates in the checked file. No timestamps or session ids: random splits are optimistic.

#### SciDB Raspberry Pi devices (8 clients)
- 31 features + label 0..8 (benign + 8 malware conditions), all conditions on all devices. Files: 8 device CSVs (20,448-21,765 rows), `merged_df.csv` = exactly 2 x the device rows (342,106).
- **Each feature vector repeats about 5x within a device (about 33,354 unique rows of 342,106 in the merged file)**; de-duplicated before splitting. Some conditions have far fewer unique windows (dev1 cond2 235, dev7 cond1/2 229/251).
- Release is globally min-max normalised; no timestamps, run or session ids: 30-s windows from one infection run remain dependent across splits (unfixable here).
- Calibration pools are small (about 69 benign windows per device).

#### What the audit implies
Contamination is injected (random, stealth, top-score) at controlled rates; only the market prevalence structure is natural. Duplicates and package overlap are measurable but had small effects on the AE metrics (SciDB AUC .857 raw vs .837 de-duplicated).


---

## PoC Matrix (detailed results)

*Source: `poc/PoC Matrix.md` (merged 2026-09-24). *

### CASE-Android PoC Matrix

Audit snapshot: 2026-09-23. Results are exploratory and do not freeze the future confirmatory protocol. Exact inputs are outside this repository where noted.

| ID | Lane / idea | Scientific question and data | PoC / baseline | Best valid result and uncertainty | Main failure modes / compute | Decision and roadmap impact | Evidence |
|---|---|---|---|---|---|---|---|
| S1 | SELENE artifact contract and pairability | Can the local released compact data instantiate the current estimand? | Inspected all eight local Parquet schemas, row counts, hashes, versions, run duration and joins. | 44,485 A10 and 30,751 A14 runs; 30,746 exact SHA pairs; 22,475 paired package groups; 13,739 A10-only and 5 A14-only. | Compact semantic features only; no event/lifecycle/context-representation files in local cache. CPU-only, 29.78 MB input. | **PROMOTE.** Keep SELENE primary; state population and context limits. | [Dataset Audit](Dataset%20Audit.md); `temp/progress.md` |
| S2 | Paired-population selection bias | Does the paired population differ from A10-only runs? | Compare each of the 19 source-flag prevalences. | Macro absolute difference 3.20 pp; maximum 8.72 pp. | One-sided comparison: only five A14-only cases; selection mechanism unknown. | **PROMOTE.** Make this population audit mandatory and define scope to paired APKs. | `temp/selene_poc_results.json` → `dataset_audit` |
| S3 | SELENE directional recurrence | How much recorded evidence recurs? | Source-positive APK × flag claim, exact hash pair; both directions. | 10→14: 377,800 claims, 89.96% recurrence. 14→10: 397,626, 85.48%. Indicator recurrence ranges widely; minimum includes process-memory-read (46.1% / 49.2%), while some flags exceed 99%. | Pooled micro rate is dominated by common flags. Some indicators are exact duplicates/threshold derivatives. | **PROMOTE.** Keep directional and four-state/indicator macro analysis; do not use one pooled number alone. | `temp/selene_poc_results.json` → `support_by_indicator` |
| S4 | SELENE model ladder: global, persistence, Boolean, count, volume, rich | Does source-side information help beyond trivial recurrence priors? | One 80/20 package-grouped holdout, seeds 20260923 and 314159; pooled L2 logistic with claim identity. Same rows per comparison. | Rich Brier vs indicator-persistence: 10→14 0.06349/0.06414 vs 0.07033/0.07025 (Brier skill 9.74%/8.69%); reverse 0.10226/0.09554 vs 0.10675/0.09953 (4.21%/4.01%). Rich AP is high (0.958–0.988) but prevalence is high too. Package bootstrap 95% intervals for absolute Brier improvement are above zero on both splits/directions. | Two split seeds only; exploratory holdout, not OOF or independent replication. 300 cluster bootstrap draws, not final 10,000. | **PROMOTE.** Keep persistence as the main baseline; use pooled logistic as primary candidate. Gains are modest and asymmetric, so claim only incremental signal. | `temp/selene_poc_results.json`; `temp/selene_poc_split314159.json` |
| S5 | Evidence-strength versus Boolean; exposure normalization | Do counts add value, and does raw duration explain it? | Evidence-count-only, volume-only, raw rich, duration-normalized count and rich-normalized variants. | Raw evidence counts slightly improve on Boolean (Brier 0.06420 vs 0.06458 forward; 0.10301 vs 0.10453 reverse, seed 20260923). Rich raw beats rich normalized counts in all four direction/split cells (e.g., forward 0.06349 vs 0.06378; reverse 0.10226 vs 0.10253). Count-only outperforms normalized count-only. | Counts are correlated/redundant; run durations differ between contexts. Normalization denominator is elapsed source duration, not active time; one normalization is not exhaustive. | **PROMOTE_AS_SECONDARY.** Add raw and predeclared duration-normalized evidence-count sensitivity; do not claim strong strength effect until a frozen confirmation. | Same SELENE result JSONs, all models recorded in `holdout_metrics` |
| S6 | Broken-pair control | Does genuine APK pairing matter? | Keep source rows and predictions fixed; permute held-out target APK rows. Compare models against training-only persistence. | Under genuine pairing, rich improves Brier over persistence. Under broken pairing, CASE models are worse than persistence in both directions/seeds (absolute improvements roughly −0.002 to −0.008). | One permutation seed per split; broken pairing also changes app-level target distribution. | **PROMOTE.** Mandatory integrity gate; retain additional permutations in the frozen run. | SELENE result JSONs → `broken_pair_negative_control` |
| S7 | SELENE selective-reporting / report proxy | Does ranking allow lower non-recurrence at fixed coverage? | Descriptive top 50% / 80% held-out score coverage, persistence versus Boolean/rich. APK report proxy: unsupported claims per report and reports with any. | At 50%, rich non-recurrence risk: 0.30% forward; 3.07–3.35% reverse. Persistence is worse in these holdouts; at 80%, differences are smaller. Rich cuts unsupported claims/report and report-any rate in both directions. | Coverage is selected from held-out scores, not a calibration-only threshold; therefore this is descriptive, not a deployable/risk-controlled policy. Base rates are very high. | **PROMOTE_AS_SECONDARY.** Keep risk/coverage and report proxy; require calibration-only threshold selection before any policy claim. | SELENE result JSONs → `descriptive_selective_and_report_metrics` |
| K1 | KronoDroid artifact/schema/identity audit | Are emulator and real-device samples actually pairable and comparable? | Stream four public CSV zip files; inspect rows, columns, hashes, duplicates, labels, missingness, and schema. | 63,316 unique one-to-one SHA pairs after excluding duplicates; labels 34,977 benign / 28,339 malware; 288 syscall columns align with no missing dynamic cells. Family labels disagree on 33,838 pairs; actual 484-column files do not reconcile to README’s 200+289 count. | About 25.68 MB compressed; raw CSVs streamed only in memory. Static metadata schema/date fields differ. Peak RAM unmeasured. | **PROMOTE_AS_SECONDARY.** Use exact-hash subset; exclude duplicates, audit schema mismatch, do not use families. | `temp/kronodroid_audit.py`; `temp/kronodroid_audit.json` |
| K2 | KronoDroid paired portability | Is there a descriptive same-APK emulator/device syscall recurrence signal? | 288 syscall occurrence claims, both directions; random target-row pairing control; malware/benign strata. | Emulator→device 59.14% of 2.328M source-positive claims; reverse 68.59% of 2.007M. Broken pairs: 51.84% / 60.11%, respectively. Support≥100 syscall macro recurrence: 50.04% / 57.70%. | Does not estimate a source-only predictive model or a security-report outcome; syscall calls are not semantic security claims. | **PROMOTE_AS_SECONDARY / CORE REPLICATION.** Use as context-family replication and negative-control replication, with narrow semantics. | `temp/kronodroid_audit.json` → `syscall_portability_poC` |
| A1 | AndroCT method/class/package portability | Does abstraction trade detail for portability? | Dataset and README/license/size audited; no tarball processed. | Release nominally claims 35,974 paired apps and exact method-call edges, but actual matching/duplicate counts are unverified. | 6.3 GB; agreement required from faculty/permanent staff, no redistribution/commercial use. Direct novelty collision. | **DEFER / BLOCKED.** Only add after access terms and exact pair manifest are cleared. | [Dataset Audit](Dataset%20Audit.md); [Zenodo](https://zenodo.org/records/6336104) |
| D1 | DYNAMISM same-context repeatability | How much instability is ordinary execution stochasticity? | Metadata audit only; record is Restricted. | Metadata claims 1,821 second-run apps (1,049 benign/772 malware), Android 9, one-second features. No pair rows directly inspected. | Files unavailable; run failures class-skewed. | **DIAGNOSTIC / BLOCKED.** Seek authorized access; do not make CASE depend on it. | [Zenodo record](https://zenodo.org/records/21280255) |
| C1 | CIC reboot stages | Can install/pre-reboot/post-reboot claims be paired? | Official page audited; no local data or identity manifest. | Stage design is promising, but advertised totals conflict (5,000 vs 5,491). | Identity, comparable feature table and license not validated. | **BLOCKED.** Do not write a reboot estimand into the core until exact artifact join passes. | [Official dataset page](https://www.unb.ca/cic/datasets/invesandmal2019.html) |
| T1 | TraceDroid / historical trend | Can older per-year profiles support portability drift? | Local inventory (45.18 MB; 85 entries) and dataset metadata checked. | Historical single-context profiles; no paired same-APK target context established. | APK cohort year/date semantics are not target-context observation dates. | **DEFER.** May support a separate cohort drift study, not CASE recurrence. | [Zenodo record](https://zenodo.org/records/3665877) |
| N1 | Novelty collision screen | Is context-dependence itself new? | Search primary dataset cards, papers, and method literature. | Direct collisions found for context dependence, emulator/device inconsistency, historical malware drift, Android behavior abstraction, selective prediction, and risk control. | Search is targeted, not systematic. | **PROMOTE** residual novelty only: claim-level recorded recurrence plus prospective report scoping. No “first” claim. | [Novelty Audit](Novelty%20Audit.md) |
| O1 | Temporal/family/claim OOD | Do models generalize to time, family, or unseen claims? | Audited actual available timestamps/labels and pairing. | SELENE has only two environments; Krono family labels conflict on 53.4% of valid paired hashes and its time fields are unverified APK metadata. | Current data semantics fail to establish valid time/family holdouts. | **DEFER.** New primary/artifact audit required before these become claims. | [Dataset Audit](Dataset%20Audit.md) |

#### P11 — deployment-motivated Android context-client FL PoC

| Field | Protocol / finding |
|---|---|
| Dataset and task | KronoDroid public CSV ZIP release; binary Android malware classification from 288 aligned released syscall-count features after excluding aggregate `nr_syscalls`; 63,316 unique exact paired APK hashes (34,977 benign, 28,339 malware). |
| Client construction | Two **simulated context clients**: emulator acquisition and physical-device collection. This is deployment-motivated context heterogeneity; these are not independent data owners, IoT network endpoints, or privacy silos. Same exact APK appears at both contexts. |
| Leakage control | Global APK-hash-grouped split applied to both contexts: about 70% train, 15% calibration, 15% test, independently repeated for seeds 20260923, 314159, 42. No identical APK crosses partitions. Hash is grouping key only, never a predictor. Family field excluded because labels conflict on 33,838 paired hashes. |
| Preprocessing and FL | `log1p` syscall counts; pooled train-only standardization; linear logistic model centrally and locally; CPU PyTorch linear model, FedAvg/FedProx, 12 rounds, one local epoch, batch 1,024, SGD at 0.03, FedProx μ=0.1. The pooled scaler and absent secure aggregation mean this is not a privacy-preserving deployment implementation. |
| Heterogeneity | Descriptive full-paired-corpus statistics (not a training-only estimate): malware prevalence is identical in both contexts by paired construction (gap 0); mean absolute gap in per-syscall presence rates is 0.0825, max 0.9797; mean Bernoulli JS divergence 0.0364 nats; paired per-feature presence agreement mean 0.9133. |
| Metrics | Macro and worst-client balanced accuracy (BA), macro Brier, client-wise AUC, false-positive/false-negative rates, and client disparity; global and client-specific calibration thresholds selected only on the disjoint calibration APKs. |
| Results across 3 seeds | Mean macro BA / macro Brier: centralized **0.806 / 0.135**; local-only **0.823 / 0.123**; FedAvg **0.759 / 0.158**; FedProx **0.758 / 0.159**; opposite-context transfer **0.508 / 0.411**; FedAvg with CASE top-half presence-agreement mask **0.500 / 0.247**. Local-only BA exceeds FedAvg by 6.4 percentage points. The transfer failure is severe; the FL baselines do not outperform local models in this two-domain POC. |
| CASE signal test | The train-only top-half feature mask scores paired binary-presence agreement across APKs, but the 144 selected features have mean agreement ≈1.000 and are mostly invariant zero features; the mask collapses to chance. This rejects this simple agreement-only filter. Calibration thresholding changes macro BA by less than 0.2 points in these runs and does not improve macro Brier. |
| Decision | **PHD_PARTIAL; diagnostic negative result, not a candidate promotion.** Real paired observations support a bounded Android context heterogeneity question, but the POC provides no IoT network, independent client owner, privacy mechanism, or advantage over local-only training. Do not claim federated improvement. |
| Artifacts | Runner: [`temp/kronodroid_fl_context_poc.py`](temp/kronodroid_fl_context_poc.py); aggregate-only metrics: [`temp/kronodroid_fl_context_poc_results.json`](temp/kronodroid_fl_context_poc_results.json). CSV archives were downloaded in memory; no sample-level rows are stored in this audit. |

The result is exploratory: three random identity splits do not supply independent external datasets; paired contexts duplicate APK identities across clients within each partition by design; endpoint costs, communication, privacy leakage, malicious clients and temporal drift were not measured. The local/global/FedAvg/FedProx baselines use compatible identity splits. The CASE mask is deliberately reported as a failed candidate baseline, not hidden or tuned on the test data.

#### Explicit split and metric interpretation

- SELENE splits group `package_name`, so all claims for a package remain together. The holdout seeds are alternative grouped manifests; they are not independent datasets.
- The label is whether the paired target context has the same released processed flag. It is not behavior truth, malware status, or proof of capability.
- Brier, log loss, and average precision are computed on the same source-positive held-out claims. Because recurrence prevalence is 85–90%, AP must be read against persistence and prevalence baselines.
- Bootstrap intervals use 300 package-cluster resamples as a discovery-stage stability check. The confirmatory design should use the roadmap’s larger frozen bootstrap and the same package resample for paired comparisons.
- Fixed coverage uses held-out score ranking. It is a retrospective risk/coverage description, not a calibration-only policy and not a formal risk guarantee.
- The two SELENE seeds, count/volume model variants, normalized-count variants, and negative controls are all retained in `temp/`; nothing was selected by deleting an unfavorable run.


---

### Phase 3 addendum — detailed PoC results (2026-09-23)

Common protocol: package-grouped 60/15/25 splits (hash of `pkg_name`+seed) shared across clients; train cap NTR, val = max(60, min(1500, NTR/3)), test ≤ 4000, natural prevalence; MLP 920→128→1, Adam 1e-3; local 20 epochs; central 20 epochs on pooled train; FedAvg/FedProx (μ=0.05) 30 rounds × 3 local epochs, all clients each round; FedAvg+FT = 3 epochs at 5e-4; cluster = average-linkage on symmetrised local-model transfer AUC (k=2), FedAvg inside cluster; shrink = logit blend w=n/(n+500); val-select = arm with best val BA at its val-tuned threshold; oracle = best test arm per client (upper bound only, uses test). Thresholds: fixed 0.5, global (pooled val), local (client val, BA-optimal), shrunk (blend of local/global with weight n/(n+300)).

#### Config A (7 markets) — extra numbers
- Worst-client FPR, FedAvg+FT n=1000: fixed .442, global .438, local .239, shrunk .341; FPR sd .167 / .166 / .071 / .120. n=5000: .309 / .283 / .154 / .179.
- Config B (11 clients): same ordering; n=100 local-only BA .752 (worst .541) vs central .830, FedAvg+FT .800, shrink .801, val-select .809.
- Val-select vs FedAvg+FT (A): +0.0115 (se .0029) at n=1000; −0.0023 at n=100; ≈0 at 5000. Chosen arms at n=1000: shrink 15, local 8, cluster 6, fedavg 4, FT 2 (of 35).
- Gating (LOSO-seed logistic): A 140 client-runs, FT>local in 60 %; B 220 runs, 66 %; N-BaIoT 144 runs, 84 %.
- Heterogeneity measures: prevalence gap, feature JSD to pool, transfer-in gap, own-AUC, n — Spearman with FT−local gain: log n −0.34/−0.35, transfer gap +0.16/+0.22, JSD +0.18/−0.10, prevalence gap +0.25/+0.15, own-AUC −0.27/−0.36.

#### Prior vs score shift (P52)
FedAvg at n=1000: fixed BA .847 (worst FPR .324); prior-corrected .802 (.636); local-tuned .864 (.253). Same direction for FedAvg+FT and central at n=300/1000/5000.

#### Abstention, robustness, cold start (seeds 1-4, n=1000)
- Abstention, FedAvg, 80 % target coverage: client cutoffs coverage sd .023, error .059, worst-client error .120; global cutoff sd .069, error .057, worst .105. FedAvg+FT: .046/.087 vs .044/.066.
- Robust aggregation: honest BA (mean of non-attacker clients) — mean .845 no attack, .825 with 1mobile flipped, .837 appchina, .835 play; median .838/.822/.831/.841; trimmed .842/.828/.843/.839; Krum .744/.711/.758/.773 (honest-client rejection). Trimmed-mean outlier share, no attack: Anzhi .46, Play .40, PlayDrone .24, 1mobile .15; attacker 1mobile .25.
- Cold start (leave-one-market-out, 4 seeds): k=100 shots: global fixed .786 / worst .611; global+FT .841 / .739; local-on-shots .830 / .782; nearest-3 .774; route-3 (k-shot thr) .812 / .680. k=20: global+FT .825 / .692; local-on-shots .755 / .670.

#### N-BaIoT replication (9 devices, 5 seeds)
n=1000: local-thr central .997/.989, FedAvg .998/.991, FedProx .997/.989, FT .997/.991, local-only .990/.945; val-select .992 (oracle .998). n=100: local-only .956/.859, FedAvg .987, FedProx .991, val-select .959. Saturated; no scope decision matters at n ≥ 1000.

#### Failures kept
Transfer-graph ensemble negative; routing negative; heterogeneity gate no better than always-FT; shrunk thresholds never better than local; abstention client-specific worse at worst client; seed-0 extras output lost (rerun launched but not saved).


#### Phase 5 (2026-09-24) — details

Policies (per client threshold at alpha from a pool of N benign-labelled scores with injected contaminants): LOCAL, GLOBAL (mean of local thresholds), ORACLE-TRUE (drop true contaminants), ORACLE-TRIM, PEER-TRIM (poc2 estimator), CLEAN-K (k verified-benign items only), CLEAN-K-GLOBAL / -SHRINK(.25,.5,.75), LAB-LOCAL (per-client Label-Trim), LAB-POOL, LAB-EB (beta-binomial method-of-moments), LAB-EB-V (drop verified contaminants), LAB-EB-TAU (trim eps-hat x tau), LAB-EB-ALLOC, PEER+LAB, HYB (mean of LAB-EB-TAU and CLEAN-K-SHRINK). 30 resamples x 5 seeds; success = FPR<=2alpha and TPR>=.8 oracle TPR per client. Budget k: 0,5,10,20,50. Key numbers in `Final Report.md`; full tables in `temp/p5_analyze_output.txt`, `temp/p5_final_output.txt`. Known limits: injected contamination; verified sample is uniform random; one scorer per domain frozen; alpha=.01 unresolved by pool size; HYB was defined post hoc (exploratory).


---

## POC Matrix (index and dispositions)

*Source: `poc/POC Matrix.md` (merged 2026-09-24). *

### Required POC Matrix Index and Formulation Dispositions

Audit date: 2026-09-23. This is the canonical matrix for the 2025–2026 formulation audit under hard gate G-PHD. The numerical SELENE and KronoDroid results remain in [PoC Matrix.md](PoC%20Matrix.md); this file records why federation designs did or did not receive a model POC. P11 now has a three-seed CPU context-client POC; its negative outcomes are retained. **No P1–P25 candidate passes all hard project eligibility checks.**

#### POC disposition by scope family

| Lane IDs | POC attempted / minimum discriminating test | Result / status | Why this is enough or what blocks execution | Recommendation |
|---|---|---|---|---|
| A01 / P1 | SELENE pairability; two package-grouped seeds; global, persistence, Boolean/count/volume/rich logistic; broken-pair negative control; descriptive risk/coverage | Positive modest Brier skill in both directions and seeds; broken-pair advantage disappears. Existing results, not rerun. | Directly instantiates the target estimand and source-only baseline comparison. Results exploratory; calibration policy and external model-level replication remain open. | PROMOTE, bounded |
| A12 / P2 | Same as A01 plus feature evidence strength and four-state/emergence analysis; KronoDroid exact-SHA descriptive replication | Counts add small consistent gain; raw features outperform tested duration normalization. Four states show target-recorded emergence separately. Krono reproduces paired context association on a non-equivalent syscall unit. | These outputs already exist in `temp/selene_poc.py`, JSON outputs and `kronodroid_audit.py`. No new run necessary. | PROMOTE_AS_EXTENSION; collectively preferred P2 |
| A02 / A03 / B01 / B02 / B11 | Natural client inventory and minimal client-definition POC | **BLOCKED_GROUPING / BLOCKED_ACCESS.** No multi-owner source-positive paired claim data or authorized organizational clients identified. | SELENE and Krono are single published datasets with APK rows observed in two environments; APK shards are not owners. Cross-context pairs shared by APK make naive client splits leak identity. | BLOCKED; no synthetic FL benchmark |
| A04 / B07 / B08 | Test reliability-weighted aggregation/client selection against FedAvg, FedProx, drift-aware/reliability baselines | **BLOCKED_VALIDITY.** No clients, rounds, updates or independently available portability signal per client-round. Recent direct collisions include DCAA and FL-MalDrift. | A toy aggregator on fabricated partitions cannot resolve prior-work collision or establish deployment-time reliability. | REJECT current formulation |
| A05 / B03 / B04 | Context personalization or shared representation + context head; minimum comparison is local-only, global, personalized, worst-client | **BLOCKED_GROUPING / BLOCKED_SUPPORT.** Two contexts are domains, not independently owned clients; no third context for unseen evaluation. Personalization collisions include ADAM and AndroIDS. | SELENE provides sufficient recurrence examples but not the unit needed for FL training or worst-client claims. | DEFER |
| A06 / B06 | Context-clustered FL against arbitrary clustering and single global model | **BLOCKED_SUPPORT.** Only two named contexts; clustering them is tautological and not a meaningful unsupervised grouping test. | No feasible scientifically valid POC without inventing clusters. | REJECT |
| A07 / A08 / B04 | Domain adaptation/generalization with prospective held-out environment | **BLOCKED_SUPPORT.** Two SELENE contexts and two Krono acquisition modes do not form multiple train domains plus a third held-out domain. | Existing descriptive pair analyses do not test prospective generalized malware detection. | DEFER |
| A09 / P8 / B05 | Selective prediction and calibration | Partial descriptive POC already run: held-out score-ranked risk/coverage. | Test outcomes choose ranking cutoffs, so no calibration-only operating policy, no malware detector, no federated setting. Existing report plans a disjoint calibration partition. | Keep as CASE secondary; FL version deferred |
| A10 / P9 / B10 | Cross-silo threat-intel / summary-sharing feasibility | **BLOCKED_ACCESS / BLOCKED_GROUPING.** No actual lab/SOC/sandbox partners or compatible evidence ontology identified. | This is primarily a participant/data-governance prerequisite; algorithm code cannot make the POC valid. | Strategic future bridge; not current project |
| B09 | Prototype-sharing versus full-model exchange | Not run: no valid clients and no prototype privacy/utility question in current dataset. | A synthetic comparison would be method decoration; no data silos to protect. | REJECT current lane |
| B12 | Vertical/partial feature federation | **BLOCKED_IDENTITY / BLOCKED_GROUPING.** SELENE modalities are in one artifact; no distinct holders share aligned APK IDs/labels. | No vertical parties, identity governance or complementary owner-specific modalities. | BLOCKED |
| A11 | Context-conditioned collaborative detection | Context stratification is already covered by directional and per-indicator CASE analysis; no client training POC. | The context variable is bundled into target domain and would risk shortcut learning. | Merge into A01 diagnostics |
| IoT PhD challenger | Dataset/client metadata screen for new 2026 crowdsensing dataset; compare data organization and task to CASE | **BLOCKED_COMPUTE / BLOCKED_NOVELTY for a quantitative POC.** Science Data Bank lists 331,750,002,920 bytes total; size of minimum 32-feature per-device subset remains unavailable. DFL baseline already exists. | Potential device endpoints but one lab testbed, temporally dependent windows, global normalization to audit, scripted attack-condition semantics to verify; not Android apps. Do not download 332 GB to discover the subset. Retrieve subset manifest/size and freeze a distinct reliability/selective-risk RQ before a small POC. | Keep as separate dissertation lead; no CASE project switch |
| P11 / KronoDroid paired-context FL | 3-seed CPU identity-grouped POC; central/local/FedAvg/FedProx, cross-context transfer, CASE agreement mask and calibration | **Executed; negative for FL advantage.** Local-only mean macro BA 0.823 versus FedAvg 0.759 and FedProx 0.758; opposite-context transfer ≈0.508; the CASE top-half mask ≈0.500. Macro Brier: local 0.123, FedAvg 0.158. | Two simulated context domains only, no IoT network/owner, shared APK identities grouped together, pooled scaler, no privacy threat model. | PHD_PARTIAL diagnostic only; do not promote the method. |
| P3 / P10 | Hybrid federated portability/personalization | Not run because inputs cannot support component isolation or real clients. | Artificial client construction would invalidate the flagship premise; stacked methods also multiply existing collision risk. | Reject current evidence basis |

#### Link to existing quantitative POCs

The existing detailed SELENE/Krono matrix contains dataset identities, estimands, model variants, metrics, output paths, uncertainty and limits. It is intentionally retained rather than duplicated. Exact model outputs are under `temp/` and are named in `progress.md`; no raw sample-level data are stored in this audit directory. See also [Research Matrix.md](Research%20Matrix.md), [Dataset Audit.md](Dataset%20Audit.md), and [Claim and Gate Audit.md](Claim%20and%20Gate%20Audit.md).

#### Lane-level assessment for all requested formulations

For A01–A12, the target is a recorded Android claim in SELENE contexts A10↔A14. For FL formulations B01–B12, the required target is performance/reliability for an independent participant/client or held-out context. Because there are no valid clients, quantitative client metrics are NA, not zero. “Book fit” refers to a chapter on mobile application privacy/security; CASE is stronger for app security reporting than for privacy-enhancing computation.

| ID | Idea / question | CASE / FL / PhD / book fit | Closest work, collision and residual | Data / client / context | Minimum test, baseline, primary metric | Result, stability, limitation, added value, complexity, decision |
|---|---|---|---|---|---|---|
| A01 | Does a source-positive recorded claim recur in a named unseen paired context, and can source evidence help estimate it? | CASE core; FL unnecessary; PhD medium via heterogeneity reliability; book strong security, partial privacy | Cross-device behavior consistency and dynamic-analysis context effects collide with the broad premise. Residual is claim-level source-only recurrence plus report scoping. | SELENE exact paired APKs; no FL clients; A10/A14 bundled contexts. | Complete: persistence, Boolean and rich source-only logistic, grouped seed splits, broken pairing. Brier/log loss against persistence. | 4.0–9.7% exploratory Brier skill across directions/seeds; stable positive result but one dataset. Direct added evidence; low complexity. **PROMOTE, bounded.** |
| A02 | Can custodians collaboratively learn claim portability without pooling traces? | CASE+FL; strong PhD in principle; strong security/partial privacy book fit | Generic malware FL is occupied; exact estimand residual is plausible but data substrate absent. | No owner/client identities, no separated paired tables, no compatible cross-silo dataset. | Client inventory gate first; later compare central/local/FedAvg/FedProx on same owner-held pairs; Brier plus worst-client score. | No model POC: blocked before algorithm. Added value cannot be tested; high access/coordination complexity. **BLOCKED_ACCESS/GROUPING.** |
| A03 | Does measured evidence reliability improve collaborative malware decisions? | CASE+FL; strongest thesis link; book security fit, privacy only if threat model added | FL-MalDrift/M2FD and reliability/drift aggregation; residual would be portability reliability independent of local validation quality. | None in audited corpus has owner clients and malware-label target. | Need real client data; compare local/global/FedAvg and drift-aware baseline; per-client Brier, false-negative risk and worst-client utility. | SELENE only validates source recurrence and cannot instantiate detector or clients. High complexity; **DEFER.** |
| A04 | Should aggregation weights reflect empirical evidence portability? | Reliability + FL; strong PhD idea but poor chapter fit on current data | DCAA directly uses drift, recency and validation reliability for malware client weights; FL-MalDrift gates drifted updates. | No per-client updates or prospective reliability measurements. | Compare sample-count/FedAvg, FedProx, DCAA-like signals and portability weight on unseen natural clients; global/worst-client Brier and utility. | No valid POC; name-level new weight would be decorative. High complexity, direct collision. **REJECT current lane.** |
| A05 | Does a shared model with context-specific heads improve unseen execution-context performance? | Personalization; strong PhD, strong security/partial privacy book | ADAM and AndroIDS collide with Android/mobile personalization. Residual is claim-level worst-context scope but requires real contexts. | SELENE has only two domains, not clients; package identity crosses contexts. | Need 3+ contexts/clients; local, global and personalized head; worst-context Brier/risk and mean utility. | No valid grouping or held-out domain. Complexity medium; **BLOCKED_GROUPING/SUPPORT.** |
| A06 | Can context-similar clients be clustered to improve collaboration? | FL; PhD fit in principle; book peripheral | Clustered FL is established; two labels make clustering tautological. | No client set; only two SELENE contexts and one dataset acquisition mode pair in Krono. | Need several owners/domains; compare global FL, personalized FL, learned cluster vs random cluster; worst-client Brier. | No scientifically valid POC. High complexity with no added value identifiable. **REJECT.** |
| A07 | Can federated adaptation improve an observed target domain? | FL adaptation; PhD strong in principle; book medium | Federated adaptation mature; domain label is not participant identity. | No owner-held unlabeled target pool. | Need actual target clients and distinct owner context; FedAvg/FedProx/local adaptation; target-client Brier and detection utility. | Blocked by client/access; no result stability assessable. **DEFER.** |
| A08 | Can federated training generalize to a prospectively unseen execution context? | FL generalization; PhD strong; book medium | FDG methods mature; Android context-effect prior work. Residual requires additional context families and malware label compatibility. | Only two named SELENE contexts; Krono emulator/device; no third held-out context family. | At least 3 train/testable natural domains; compare FedAvg, DG method, local and pooled baselines; held-out-domain score. | Insufficient support; no valid POC. High data demand. **BLOCKED_SUPPORT.** |
| A09 | Can a collaborative malware detector abstain or scope under uncertainty? | FL selective detection; PhD strong; book strong mobile security | Selective prediction and federated calibration/conformal methods collide. Residual would be valid cluster/shift-aware malware risk. | SELENE is not a malware classifier; no FL clients. | Later compare global/local selective policies; calibration-only thresholds; risk at coverage and worst-client false-negative risk. | CASE has only retrospective score-ranked risk/coverage, no valid FL policy. **KEEP CASE policy question; defer FL.** |
| A10 | Can participants exchange reliability/provenance summaries as threat intelligence? | Collaborative security; strongest potential PhD bridge; book fit strong | CTI sharing and cross-silo FL exist; no common semantic claim ontology identified. | Requires 3+ independent sandbox/SOC/lab owners; none currently secured. | Partner/ontology/access feasibility study; compare local silo decisions vs shared summaries; cross-owner calibration/risk. | Blocked by access/governance; potentially high value but not feasible today; complexity high. **BLOCKED_ACCESS.** |
| A11 | Does context-conditioned collaboration beat unconditioned transfer? | CASE+FL; medium PhD; book security fit | Context-conditioned predictors and context-specific adaptation are established; shortcut risk is high. | Context is a target domain label in SELENE, not an independent client feature. | Need independent device/context descriptors and held-out family; compare conditioned/unconditioned; target context Brier. | Existing directional analysis covers descriptive contrast; no valid collaborative POC. **MERGE into A01 diagnostics.** |
| A12 | Which reliability layer actually adds information: observation, feature, detector, report? | CASE reliability architecture; PhD medium; book strong security | Reliability layers each have prior art; stack risks decoration. | SELENE has processed flags, counts and small fidelity sample; no repeated-run truth or client data. | Existing POC covers flag/count recurrence and report proxy; compare same persistence/logistic baselines with Brier and report risk. | Evidence only supports claim recurrence and exploratory report proxy. Added value in four-state + feature increment; layered architecture too complex. **NARROW/PROMOTE extension.** |
| B01 | FedAvg baseline: does collaboration beat local models under valid clients? | FL baseline, not novelty; PhD high; chapter security medium | Standard and Android malware FL prior art. Residual requires independent real clients. | No valid multi-owner clients. | Central/local/FedAvg; macro client Brier, AUPRC and worst-client risk. | Blocked client validity; no result. **BLOCKED.** |
| B02 | FedProx under true feature/context heterogeneity? | FL baseline; PhD high; chapter medium | Standard heterogeneity baseline; not novel alone. | Same absent client pool. | FedAvg vs FedProx vs local/global on same clients; worst-client Brier and convergence. | No valid benchmark; **DEFER.** |
| B03 | Personalized local heads? | FL personalization; PhD high; book medium | ADAM/AndroIDS already collide. | No real context clients, only two outcome domains. | Compare local/global/personalized and worst-client risk across 3+ contexts. | Invalid current clients; **BLOCKED.** |
| B04 | Shared representation + context head? | Context-aware FL; PhD high; book medium | PFL/FDG and context-conditioned methods. | No owners or context support for held-out evaluation. | Shared vs local head, FedAvg and local; held-out-context Brier. | No valid result; **DEFER.** |
| B05 | Local calibration over globally shared detector? | FL calibration; PhD high; book strong security/partial privacy | FedCP and other federated calibration methods; guarantee assumptions matter. | No client calibration partitions or malware outputs. | Fed local calibration/global calibration/no calibration; calibrated Brier, coverage/risk by owner. | Existing CASE test-ranked diagnostics are not local calibration; no FL POC. **DEFER.** |
| B06 | Cluster FL by learned behavior/context? | FL grouping; PhD high; book medium | Clustered FL established; two SELENE contexts are not enough. | No meaningful client graph/group population. | Learned vs random vs no clusters, using train-only similarity; held-out group worst-client metric. | No valid clusters; **REJECT.** |
| B07 | Portability-aware aggregation? | FL reliability mechanism; PhD high; book peripheral | DCAA direct collision, plus FL-MalDrift; residual only if independent signal and gain. | No client-round update data. | Compare FedAvg/FedProx/DCAA-like/portability aggregation; held-out owner Brier and worst-client utility. | No honest current POC; **REJECT current formulation.** |
| B08 | Reliability-aware participant selection? | FL availability/control; PhD high; book peripheral | Drift-aware participation already exists. | No participant rounds or dropout data. | FedAvg vs drift selection vs reliability selection; utility/coverage, excluded-client harm and worst-client score. | No participant table; **DEFER.** |
| B09 | Share class prototypes instead of full model updates? | Privacy/collaboration; PhD medium-high; book privacy interest | Prototype exchange is mature; no owner data or privacy proof. | No separate participants or aligned class distributions. | Compare full FL/prototypes/local; utility, bytes and membership/attribute leakage. | No meaningful client exchange; **REJECT current lane.** |
| B10 | Distill or exchange behavior summaries? | CTI/FL; PhD high; book security/privacy | Distillation, summaries and CTI existing; exact summary privacy needs threat model. | No multiple evidence owners. | Compare local/global/distilled; Brier plus communication and summary leakage tests. | Access/ontology missing; **DEFER.** |
| B11 | Cross-silo labs, SOCs or sandbox providers? | Natural FL; PhD high; book high | Cross-silo malware FL/CTI prior art. | No identified/authorized organizations. | Partner/data agreement audit, then leave-one-owner-out central/FedAvg/local evaluation. | BLOCKED_ACCESS; no client result; high coordination cost. **BLOCKED.** |
| B12 | Vertical/partial-feature FL across evidence holders? | Privacy/collaboration; PhD high; book strong | Vertical FL mature; no separate evidence custodians identified. | SELENE holds features together; no external modality provider with shared APK identities. | Verify parties, aligned IDs and feature ownership; central vs vertical baseline, utility and linkage leakage. | Identity/data ownership absent; **BLOCKED_IDENTITY/GROUPING.** |

#### Overall POC decision

Completed POCs include A01/P1 and A12/P2 (CASE-only) and P11 (deployment-motivated paired-context Android malware FL). P11 provides an exploratory two-context client experiment, not a natural-owner or IoT-network benchmark; its negative result rules out the tested simple CASE portability mask and shows FedAvg/FedProx below local-only performance. The strict G-PHD gate yields no eligible candidate. Other FL experiments remain blocked by absent natural/valid client settings, unavailable participants, incompatible outcomes, or insufficient independent contexts. No arbitrary APK shards were used.


---

### Phase 3 addendum — POC index (canonical index; details in `PoC Matrix.md`)

| POC | Lane(s) | Script → output | Seeds | Verdict |
|---|---|---|---|---|
| P3-1 market join | P46 | `p3_lamda_markets.py`, `p3_build_lamda.py` → `p3_lamda_markets_summary.json` | – | all 1,008,381 hashes joined; 7 usable clients |
| P3-2 pilot (v1) | P26/P30 | `p3_lamda_collab_poc.py` → `p3_lamda_collab_poc_results_A_n*.json` | 3 | superseded by v2 (kept) |
| P3-3 collaboration-scope sweep | P26-P31,P35,P37,P47,P48,P53,P55,P57 | `p3_engine.py`, `p3_lamda_v2.py` → `p3_lamda_v2_{A,B}_n{100,300,1000,5000}.json` | 5 | see Research Matrix table |
| P3-4 gating analysis | P27,P31 | `p3_analyze.py` → `p3_analysis_output.txt` | LOSO-seed | gate ≈ always-FT |
| P3-5 threshold/transfer/extras tables | P26,P28,P30,P36,P39,P40,P44 | `p3_analyze2.py` → `p3_analysis2_output.txt` | 5 (extras 4) | see below |
| P3-6 extras | P36,P39,P40,P44 | `p3_extras.py` → `p3_extras_results_s1-4.json` | 1-4 | seed 0 run did not save output; not reported |
| P3-7 transfer graph | P29 | `p3_lamda_v2.py … _tg` → `p3_lamda_v2_A_n*_tg.json` | 5 | negative |
| P3-8 drift vs context | P38 | `p3_drift_vs_context.py` → `p3_drift_vs_context_results.json` | – | descriptive positive |
| P3-9 prior shift | P52 | `p3_prior_shift.py` → `p3_prior_shift_results.json` | 5 × 3 n | prior correction hurts |
| P3-10 IoT replication | P54,P55 | `p3_nbaiot_v2.py` → `p3_nbaiot_v2_n{100,300,1000,3000}.json` | 5 | saturated |

Not run: P32, P33, P43, P45, P50, P51, P58; P41/P42 blocked by data.


#### Phase 5 index

| ID | Script -> outputs | Seeds | Result |
|---|---|---|---|
| P59 | `p5_scores.py`, `p5_mobsup_scores.py` -> `p5_scores/`; `p5_budget.py` -> `p5_budget_{mobsup,mobile,nbaiot,scidb}_a{0.05,0.01}.json`; `p5_budget_analyze.py`, `p5_budget_final.py` -> `p5_analyze_output.txt`, `p5_final_output.txt` | 5 | failure + oracle headroom confirmed in 3 domains; LAB-EB-TAU recovers 82-93% of gap vs LOCAL; strongest simple baseline CLEAN-K-SHRINK ties/wins in heterogeneous average; runner-up |
| P60-P63 | reuse `p4_robust_output.txt`, `p3/p4` blend/transfer results | 5 | Stage 1/2 failed (no headroom); not re-implemented |
Superseded: poc2 v1-v4 PEER-TRIM claims ("best defensible") — partial recovery only.
